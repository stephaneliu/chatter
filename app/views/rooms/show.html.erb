<h1>
  ActionCable Chat
</h1>

<div id="messages">
  <%= render @messages %>
</div>

<%= form_for @message, remote: true do |f| %>
  Say something:
  <%= f.text_field 'content' %>
  <%= submit_tag("Talk") %>
<% end %>
